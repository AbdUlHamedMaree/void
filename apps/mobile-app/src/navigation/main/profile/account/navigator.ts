import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { AccountStackParamList } from './model';

export const AccountStackNavigator = createNativeStackNavigator<AccountStackParamList>();

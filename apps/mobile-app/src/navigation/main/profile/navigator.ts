import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { ProfileStackParamList } from './model';

export const ProfileStackNavigator = createNativeStackNavigator<ProfileStackParamList>();
